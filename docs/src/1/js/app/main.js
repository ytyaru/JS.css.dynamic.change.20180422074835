define(function(require, exports, module) {
    var SelectBox = require('js/app/ui/SelectBox');
    //document.body.appendChild(SelectBox.Create());
    //document.body.appendChild(document.importNode(SelectBox.Create(),true));
    SelectBox.AppendTo(document.body);
    //document.getElementById('example').appendChild(document.importNode(xml.getElementsByTagName('div')[0],true));
});
